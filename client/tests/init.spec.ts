import {describe, test, expect } from '@jest/globals'
// import {usernames, dms, getChatIdGen, userIds} from '../src/features/socket/init'

describe('init', () => {

    // test('usernames', () => {
    //     expect(Object.keys(usernames).length).toEqual(10)
    //     expect(Object.keys(usernames).every(name => name.startsWith('user'))).toBeTruthy()
    // })

    // test('userIds', () => {
    //     expect(Object.keys(userIds).length).toEqual(10)
    // })

    // test('dms', () => {
    //     expect(Object.keys(dms).length).toEqual(9)
    // })

    // test('getChatIdGen', () => {
    //     const fn = getChatIdGen()

    //     expect(fn()).toEqual('0')
    //     expect(fn()).toEqual('1')
    //     expect(fn()).toEqual('2')
    // })

})
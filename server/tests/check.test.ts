import { describe, test, expect } from "@jest/globals";

describe('checking', () => {

    test('hello', () => {
        expect(true).toEqual(true)
        const x = 1
        expect(x).toEqual(1)
    }
)

})
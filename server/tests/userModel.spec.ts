import {describe, test, expect} from "@jest/globals"
// import UserModel from "@models/UserModel"

describe('userModel', () => {


    // test("get new", async() => {
    //     const user = UserModel.create('max', 'hashed') 
    //     expect(user.username).toEqual('max')
    //     expect(user.hashed).toEqual('hashed')
    // })


})